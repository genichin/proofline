---
id: ac-0019
status: retired
---

# Package와 user harness update의 fail-closed 정합성

## Criterion

이 AC의 current-state preflight, 사용자 파일·symlink 보호, mutation ordering, package installation 실패 시 harness 무변경과 harness commit 실패 시 verified previous package rollback 조건은 verified update 전체를 소유하는 `ac-0012`에 통합되며, `req-0012` 승인 시 이 AC는 중복 실패 조건으로 retire된다. 기존 ID와 canonical path는 Line 0011의 승인·구현·검증 evidence를 보존하기 위해 유지한다.

## Verification

- 성공 조건: `ac-0012`가 valid old state만 coherent target으로 전환하고 모든 preflight·mutation failure에서 coherent state를 보존·복원하며 `ac-0019`는 canonical path에서 `retired` 상태로 보존되어야 한다.
- 실패·경계 조건: Preflight·rollback·coherence 조건 누락, 파일 삭제·이동·ID 변경 또는 Line 0011 historical reference 단절이 있으면 retirement는 실패해야 한다.
- 검사 방법: 두 AC semantic diff, 직접 참조 검색, Git history와 `proofline validate` 결과를 확인한다.
