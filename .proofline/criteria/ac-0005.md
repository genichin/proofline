---
id: ac-0005
status: retired
---

# 충돌 없는 사전 검사와 원자적 쓰기

## Criterion

이 AC의 Line init preflight·dry-run·no-overwrite·atomic failure 조건은 동일한 제품 동작을 소유하는 `ac-0004`에 통합되며, `req-0012` 승인 시 이 AC는 중복 사양으로 retire된다. 기존 ID와 canonical path는 Line 0002의 승인·구현·검증 evidence를 보존하기 위해 유지한다.

## Verification

- 성공 조건: `ac-0004`가 이 AC의 모든 정상·실패·경계 조건을 포함하고 `ac-0005`는 canonical path에서 `retired` 상태로 보존되어야 한다.
- 실패·경계 조건: 기존 조건 누락, 파일 삭제·이동·ID 변경 또는 historical reference 단절이 있으면 retirement는 실패해야 한다.
- 검사 방법: 두 AC semantic diff, 직접 참조 검색, Git history와 `proofline validate` 결과를 확인한다.
