---
id: ac-0018
status: draft
---

# CLI와 user harness의 동일 update target 수렴

## Criterion

이 AC의 CLI·package·manifest·managed resource target convergence, `--check`·already-current 무변경 및 project isolation 조건은 verified update 전체를 소유하는 `ac-0012`에 통합되며, `req-0012` 승인 시 이 AC는 중복 성공 조건으로 retire된다. 기존 ID와 canonical path는 Line 0011의 승인·구현·검증 evidence를 보존하기 위해 유지한다.

## Verification

- 성공 조건: `ac-0012`가 absent·existing harness의 package와 managed resource 동일 target 수렴 및 read-only 경로를 포함하고 `ac-0018`은 canonical path에서 `retired` 상태로 보존되어야 한다.
- 실패·경계 조건: 수렴·checksum·project no-mutation 조건 누락, 파일 삭제·이동·ID 변경 또는 Line 0011 historical reference 단절이 있으면 retirement는 실패해야 한다.
- 검사 방법: 두 AC semantic diff, 직접 참조 검색, Git history와 `proofline validate` 결과를 확인한다.
