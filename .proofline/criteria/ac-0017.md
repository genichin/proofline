---
id: ac-0017
status: draft
---

# User harness 초기화의 fail-closed와 idempotency

## Criterion

이 AC의 idempotency·fail-closed·atomicity 조건은 명시적 user harness initialization을 소유하는 `ac-0016`에 통합되며, `req-0012` 승인 시 이 AC는 중복 안전 조건으로 retire된다. 기존 ID와 canonical path는 Line 0010의 승인·구현·검증 evidence를 보존하기 위해 유지한다.

## Verification

- 성공 조건: `ac-0016`이 exact rerun no-op와 mismatch·unexpected entry·symlink·concurrent target·write failure의 무변경 조건을 모두 포함하고 `ac-0017`은 canonical path에서 `retired` 상태로 보존되어야 한다.
- 실패·경계 조건: 기존 안전 조건 누락, 파일 삭제·이동·ID 변경 또는 Line 0010 historical reference 단절이 있으면 retirement는 실패해야 한다.
- 검사 방법: 두 AC semantic diff, 직접 참조 검색, Git history와 `proofline validate` 결과를 확인한다.
