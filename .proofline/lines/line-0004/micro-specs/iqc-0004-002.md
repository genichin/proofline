---
id: iqc-0004-002
micro_spec: ms-0004-002
micro_spec_commit: "7ca50c8cb50225357a1043c25ceb90ec419bc103"
implementation_commit: "8dadb465b793c135f34ad0cf4b581edb653f6019"
result: passed
---

# IQC: 공용 ProofLine uv tool environment workflow

## Target

- `ms-0004-002`와 `ac-0008`
- 선택된 source checkout의 non-editable `uv tool` installation과 executable/module provenance
- Checkout 밖 적용 project validation, application dependency·lockfile·`.venv` no-mutation과 update/release 범위 분리

## Checks

| 검사 | 실행 명령 또는 방법 | Exit code·판정 | 결과 요약 | Evidence |
| --- | --- | --- | --- | --- |
| Tool environment focused test | 공유 개발 interpreter와 branch `PYTHONPATH`로 `python -m pytest tests/test_tool_environment_skill.py tests/test_uv_tool_environment.py -q` | `0` · PASS | 5개 test 통과 | 두 test file |
| Isolated non-editable install | Temporary `UV_TOOL_DIR`·`UV_TOOL_BIN_DIR`에서 `uv tool install <implementation-checkout>` 실행 | `0` · PASS | 별도 tool venv와 executable 생성 | `tests/test_uv_tool_environment.py` |
| Installed provenance | Tool Python `-I`로 `proofline.__file__` 확인 | PASS | Module이 temporary tool directory 아래이며 source checkout 밖임 | `test_uv_tool_install_is_isolated_from_application_environment` |
| 외부 project validation | Checkout 밖 fixture에서 installed executable `proofline validate` 실행 | `0` · PASS | Canonical artifact validation 성공 | 같은 integration test |
| Application no-mutation | Validation 전후 sentinel `pyproject.toml`, `uv.lock` bytes와 `.venv` 존재 비교 | PASS | Bytes 동일, application `.venv` 생성 안 됨 | 같은 integration test |
| Skill·operation contract | Static contract tests와 Hermes SKILL.md validation | PASS | 공용 executable, non-editable install, no application mutation, Issue #9 경계 고정 | `skills/proofline-tool-environment/SKILL.md`, `docs/operations/proofline-tool-environment.md` |

## Criteria Results

| AC | 판정 | 근거 |
| --- | --- | --- |
| `ac-0008` | passed | Temporary uv tool environment의 non-editable installed executable이 checkout 밖 project를 검증했고 application project bytes와 `.venv`를 변경하지 않았으며 공용 command ownership과 Issue #9 update 경계를 문서·test로 고정했다. |

## Result

- 전체 판정: passed
- 요약: 공용 ProofLine tool environment의 install, provenance, 외부 실행과 application no-mutation 조건을 exact implementation commit에서 충족했다.
