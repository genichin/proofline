---
id: ms-0004-001
parent_req: req-0004
criteria:
  - ac-0007
spec_status: approved
implementation_status: in_progress
---

# Exact approval commit 기반 implementation worktree workflow

## Scope

### 담당 범위

- `ac-0007`의 `.worktrees/line-NNNN/` linked-worktree 경계, exact approval base, preflight, main checkout 보존과 no ProofLine venv 규칙을 구현한다.
- Repository-owned Hermes skill과 governance contract가 implementation 시작·검증·정리 절차를 소유한다.

### 제외 범위

- ProofLine CLI의 Git branch/worktree/commit/merge 명령
- Automatic main integration, delivery와 worktree 강제 삭제
- 구현 대상 project의 source build·test environment 관리

### 기준과 의존성

- Specification baseline은 `a33928860b9079fb15ec4497b0c4f753e90e6afb`이다.
- `docs/contracts/line-delivery.md`의 approval base, branch lifecycle와 fast-forward gate를 유지한다.
- `.worktrees/`는 linked worktree container이며 clone이나 canonical artifact root가 아니다.

## Implementation

### 변경 대상

- `.gitignore`
- `docs/contracts/line-delivery.md`
- `skills/proofline-start-implementation/SKILL.md`
- `tests/test_start_implementation_skill.py`

### 구현 작업

1. Skill 부재, `.worktrees/` ignore 부재, exact-base·main-preservation·no-venv 계약 부재를 검사하는 test를 먼저 작성하고 RED를 확인한다.
2. `.worktrees/`를 repository Git tracking에서 제외한다.
3. Main cleanliness, approval commit·Line/Discovery/REQ 상태, branch/path/registration 충돌을 no-mutation으로 검사하는 Hermes workflow를 작성한다.
4. Exact approval commit에서 deterministic branch와 `.worktrees/line-NNNN/` linked worktree를 생성하고 생성 후 base·branch·main checkout·ProofLine venv 미생성을 검증한다.
5. Micro-SPEC·implementation·test·IQC 작업 위치와 DQC·fast-forward·명시적 cleanup 경계를 contract에 기록한다.
6. Focused test, 전체 suite와 canonical validator를 실행한다.

### Rollback과 containment

- 실패한 preflight는 Git ref, worktree registration과 filesystem target을 변경하지 않는다.
- 생성 후 검증 실패 시 자동 강제 삭제하지 않고 실제 state와 수동 recovery 절차를 보고한다.
- 구현 rollback은 신규 skill/test와 contract 변경을 제거하고 `.gitignore` 규칙을 되돌리면 된다.

## Verification

- 검사: `/home/genichin/work/proofline/.venv/bin/python -m pytest tests/test_start_implementation_skill.py -q`, 전체 pytest, `proofline validate`, `git diff --check`를 실행한다.
- 기대 결과: exact-base workflow와 failure/no-mutation 계약이 고정되고 main checkout은 `main`에 남으며 Line worktree에 ProofLine 전용 `.venv`를 요구하지 않는다.
- Evidence: IQC artifact `iqc-0004-001.md`에 exact Micro-SPEC commit, implementation commit, command와 결과를 기록한다.
