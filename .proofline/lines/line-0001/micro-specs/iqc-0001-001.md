---
id: iqc-0001-001
micro_spec: ms-0001-001
micro_spec_commit: "16b35963833c9abafa181cc629433d77d9655528"
implementation_commit: "792aa6ec56c0a33620d98a39f72a02b34ee59183"
result: passed
---

# IQC: 최소 validator 구현

## Target

- 승인된 `ms-0001-001`과 연결된 `ac-0001`, `ac-0002`, `ac-0003`
- 실제 검증 대상 구현 commit: `792aa6ec56c0a33620d98a39f72a02b34ee59183`
- 검증 환경: Python 3.12.3, pytest 9.1.1

## Checks

- TDD RED 확인
  - 설정 오류 검사: `uv run pytest tests/test_config_validation.py -v`에서 미구현 상태의 예상 실패 3건을 확인했다.
  - CLI 판정: `uv run pytest tests/test_cli.py -v`에서 CLI module 부재로 예상 실패 2건을 확인했다.
  - 산출 문서 구조: `uv run pytest tests/test_artifact_validation.py -v`에서 미구현 규칙의 예상 실패 7건을 확인했다.
  - 직접 참조: `uv run pytest tests/test_reference_validation.py -v`에서 누락 참조 6건의 예상 실패를 확인했다.
- 전체 테스트: `uv run pytest`
  - 종료 코드: 0
  - 결과: 28 passed
- 현재 저장소 정본 검사: `uv run proofline validate`
  - 종료 코드: 0
  - 결과: 진단 없음
- 정상 최소 프로젝트 CLI 검사
  - 종료 코드: 0
  - 결과: 진단 없음
  - `test_cli_validates_minimal_fixture_without_modifying_inputs`에서 검사 전후 파일 내용이 같음을 확인했다.
- 설정 파일이 없는 프로젝트 CLI 검사
  - 종료 코드: 1
  - 진단: `proofline.yaml: config.missing: proofline.yaml 파일이 없습니다.`
- 추가 검사
  - `uv run python -m compileall -q src tests`: 종료 코드 0
  - `uv lock --check`: 종료 코드 0
  - `git diff --check`: 종료 코드 0

## Criteria Results

- `ac-0001`: passed
  - 올바른 설정과 잘못된 설정, CLI 종료 코드 및 경로·원인 진단을 검사했다.
- `ac-0002`: passed
  - YAML 머리말, 필수·알 수 없는 항목, 상태, ID·경로, H1·H2, Line 본문 금지 및 자리표시자 규칙을 검사했다.
- `ac-0003`: passed
  - REQ, Micro-SPEC, IQC 및 DQC의 직접 참조 대상 존재 여부와 안전한 정본 ID 형식을 검사했다.

## Result

`ms-0001-001`의 필수 검사와 세 AC의 판정 기준을 모두 만족하므로 IQC 결과는 `passed`이다. 이 판정은 해당 Micro-SPEC 구현에만 적용되며 Line 통합이나 delivery를 승인하지 않는다.
