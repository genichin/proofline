---
id: ms-0011-001
parent_req: req-0011
criteria:
  - ac-0018
spec_status: approved
implementation_status: implemented
---

# Target-wheel harness planning and replacement

## Scope

### 담당 범위

- Existing manifest schema/path/checksum preflight
- Target wheel resource allowlist, deterministic manifest와 staged replacement
- Absent create, existing exchange, exact target no-op와 project isolation

### 제외 범위

- Package 설치, release transport 및 user resource merge

### 기준과 의존성

- AC-0018, schema-v1 manifest, Linux `renameat2` no-replace/exchange

## Implementation

### 변경 대상

- `src/proofline/home_writer.py`
- Harness filesystem tests

### 구현 작업

1. Existing manifest와 managed tree를 self-describing checksum으로 검증한다.
2. Verified wheel의 `proofline_home` resource를 path-safe하게 읽어 target payload를 만든다.
3. Prepared transaction의 atomic create/exchange/rollback API를 구현한다.

### Rollback과 containment

- Commit 전에는 private staging만 제거한다. Exchange 후 rollback은 동일 atomic exchange로 old root를 복원한다.

## Verification

- 검사: Focused old-to-new, absent, no-op, malformed/conflict/symlink/concurrency/failure tests
- 기대 결과: Valid state만 전환되고 모든 failure에서 pre-state가 보존된다.
- Evidence: `iqc-0011-001.md`; traversal/unexpected/symlink wheel rejection, old→new exchange, create/no-op/rollback와 v0.3 legacy manifest reconciliation tests
