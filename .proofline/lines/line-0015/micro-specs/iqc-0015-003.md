---
id: iqc-0015-003
micro_spec: ms-0015-003
micro_spec_commit: 374b67b690def1630e7e639381303f3cb0f137f9
implementation_commit: d1b7d43bff56a6202a2c484e021fadb18585e8f8
result: passed
---

# IQC: Actual-filesystem race와 installed-wheel Line init E2E

## Target

`ms-0015-003`의 synchronized Linux process races, exact candidate wheel provenance, checkout 밖 dry-run·success·failure CLI boundary와 generated bytes·validation을 검증한다.

## Checks

| 검사 | 실행 명령 또는 방법 | Exit code·판정 | 결과 요약 | Evidence |
| --- | --- | --- | --- | --- |
| Packaging·installed E2E regression | `PYTHONPATH=src uv run --no-project --with pytest pytest -q tests/test_wheel_package.py` | exit 0, PASS | 2개 packaging·isolated installed-wheel test PASS; build/install `--refresh` 적용 | `tests/test_wheel_package.py`, candidate `d1b7d43bff56a6202a2c484e021fadb18585e8f8` |
| Exact wheel provenance | `uv build --refresh --wheel`; SHA-256; fresh Python 3.11 venv에 `uv pip install --refresh` | exit 0, PASS | `proofline 0.4.1`; installed module은 isolated venv `site-packages`에서 로드됨 | `/tmp/proofline-line0015-dist.RklohU/proofline-0.4.1-py3-none-any.whl`; SHA-256 `a841d68a43b9751f31cd0696f809b1a595d7f49d73ca60a210f192129680f366` |
| Checkout-outside workflow | isolated HOME·fresh Git project에서 installed `project init → line init line-0099 → validate` 실행 | exit 0, PASS | canonical Line·Discovery 두 path 생성, installed validator PASS | `/tmp/proofline-line0015-e2e.97gFHh`; candidate `d1b7d43bff56a6202a2c484e021fadb18585e8f8` |
| CLI failure·provenance matrix | isolated installed `proofline.cli.main()` write failure, non-zero result, stable stderr, no target·stage residue, source/wheel rendered bytes parity | exit 0, PASS | installed console boundary와 template provenance·cleanup 검증 | `tests/test_wheel_package.py` |
| Synchronized process race repetition | FIFO IPC target appearance·parent replacement races를 각 10회 실행 | exit 0, PASS | `fifo_process_races=10x2 PASS` | `tests/test_line_init.py` |

## Criteria Results

| AC | 판정 | 근거 |
| --- | --- | --- |
| `ac-0004` | passed | Exact installed candidate wheel이 checkout 밖에서 canonical Line·Discovery를 생성·검증하고 실제 process race와 failure cleanup을 통과했다. |

## Result

- 전체 판정: passed
- 요약: Exact candidate wheel을 commit과 digest에 결속했고 installed CLI success·failure 및 synchronized filesystem races를 검증했다.
