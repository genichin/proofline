---
id: "iqc-0019-001"
micro_spec: "ms-0019-001"
micro_spec_commit: "0efbc069d8d0cbae886c1385c04ea0af21859639"
implementation_commit: "0663066dd249ee9bcfeff6ca77ca9bb6cdc9819f"
result: passed
---

# IQC: Line 0019 Windows consumer portability

## Target

Hosted run `30985239641`의 Windows full consumer regression에서 드러난 test-harness portability 결함을 fresh correction으로 검증한다.

- Fresh implementation start `P5`: `82156976bec170b3bd1a7c5c924290162f8c0d66`
- Exact correction `I5`: `0663066dd249ee9bcfeff6ca77ca9bb6cdc9819f`
- Approved Micro-SPEC revision: `0efbc069d8d0cbae886c1385c04ea0af21859639`
- 실패한 prior candidate `V4`: `448ec3a8b393b53d07949212843284be6ec19da9`

Prior hosted run은 DQC PASS evidence가 아니며 same-`V` retry를 수행하지 않는다.

## Checks

- Hosted RED: run `30985239641`, attempt `1`; build PASS, Ubuntu PASS, Windows tracked gate PASS, Windows full consumer FAIL.
- Root causes: synthetic repository newline normalization, Windows PATH의 POSIX 강제값, text-mode CRLF fixture, POSIX-only `fcntl` collection, read-only loose-object deletion.
- TDD RED: Windows harness invariant regression FAIL.
- Correction: synthetic repositories와 regression fixture에 첫 `git add` 전 `core.autocrlf=false`를 설정한다.
- Correction: source parity PATH override는 non-Windows에만 적용하고 explicit missing-Git negative environment는 유지한다.
- Correction: EOF fixture는 raw binary LF를 쓰고, POSIX-only line-init module은 `fcntl` 부재 플랫폼에서 skip한다.
- Correction: missing-object fixture는 owner-write permission을 복원한 뒤 임시 loose object를 삭제한다.
- Focused Windows/history regression: PASS.
- Exact `I5` full regression: `593/593` PASS.
- `compileall`, `git diff --check`, `proofline validate`: PASS.
- Independent quality/security review: PASS, blocker 없음.
- Independent specification review: PASS, blocker 없음.

## Criteria Results

- `ac-0010`: PASS — Windows consumer가 exact candidate wheel과 full portable history tests를 실행할 수 있도록 harness만 교정했다. Fresh hosted run은 `V5` 이후 DQC에서 검증한다.
- `ac-0021`: PASS — source/wheel diagnostic parity와 read-only snapshot 의미를 유지하며 production validator 동작은 변경하지 않는다.

## Result

PASS — exact `I5`가 fresh `P5` 뒤에 있고 local regression·static validation·독립 review를 통과했다. Remote hosted PASS는 fresh `V5` 이후 DQC 조건으로 남는다.
