---
id: "iqc-0019-001"
micro_spec: "ms-0019-001"
micro_spec_commit: "0efbc069d8d0cbae886c1385c04ea0af21859639"
implementation_commit: "a6ee99f2e6b7e7ab29e0c1aece83f357a38ce23f"
result: passed
---

# IQC: Line 0019 exact Windows checkout bytes

## Target

Hosted run `30987259880`에서 확인된 Windows source checkout CRLF와 wheel canonical LF의 exact-byte 불일치 및 synthetic `.git` read-only cleanup 실패를 fresh correction으로 검증한다.

- Fresh implementation start `P6`: `39c4bc33a08de53eb5ec0bd99461745df57ad4c4`
- Exact correction `I6`: `a6ee99f2e6b7e7ab29e0c1aece83f357a38ce23f`
- Approved Micro-SPEC revision: `0efbc069d8d0cbae886c1385c04ea0af21859639`
- 실패한 prior candidate `V5`: `0417c208bec344d9d4d43e31badccf799a01e1ff`

Prior hosted run은 DQC PASS evidence가 아니며 same-`V` retry를 수행하지 않는다.

## Checks

- Hosted RED: run `30987259880`, attempt `1`; build PASS, Ubuntu PASS, Windows tracked gate PASS, Windows full consumer FAIL.
- Complete failed-log read-back: source checkout CRLF와 wheel LF 차이로 94 unique failures, synthetic non-Git fixture `.git` removal에서 `WinError 5` 확인.
- TDD RED: exact checkout environment와 read-only cleanup regression `2/2` FAIL.
- Correction: Windows job-level `GIT_CONFIG_COUNT/KEY/VALUE`로 첫 `actions/checkout`부터 `core.autocrlf=false`를 강제한다.
- Correction: checkout-first, exact `github.sha`, `fetch-depth: 0`, `persist-credentials: false`, checkout 직후 HEAD assertion을 유지한다.
- Correction: test-only non-Git fixture `.git` rmtree callback이 read-only path를 owner-write로 복구한 뒤 원래 삭제를 재시도한다.
- Focused Windows/history regression: PASS.
- Exact `I6` full regression: `593/593` PASS.
- `compileall`, `git diff --check`, `proofline validate`: PASS.
- Independent quality/security review: PASS, blocker 없음.
- Independent specification review: PASS, blocker 없음.

## Criteria Results

- `ac-0010`: PASS — Windows consumer source가 wheel과 같은 canonical LF bytes를 사용하며 exact-SHA/full-history/no-credential checkout 계약을 유지한다. Fresh hosted run은 `V6` 이후 DQC에서 검증한다.
- `ac-0021`: PASS — exact-byte source/wheel parity와 read-only validator 의미를 유지하며 production validator는 변경하지 않는다.

## Result

PASS — exact `I6`가 fresh `P6` 뒤에 있고 local regression·static validation·독립 review를 통과했다. Remote hosted PASS는 fresh `V6` 이후 DQC 조건으로 남는다.
