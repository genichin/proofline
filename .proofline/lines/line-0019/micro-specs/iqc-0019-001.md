---
id: "iqc-0019-001"
micro_spec: "ms-0019-001"
micro_spec_commit: "0efbc069d8d0cbae886c1385c04ea0af21859639"
implementation_commit: "9ddd62b230d924876aca8dafb5bdbd978f967cab"
result: passed
---

# IQC: Line 0019 hosted fixture stabilization

## Target

Hosted run `30984256382`에서 드러난 Windows newline normalization과 Ubuntu synthetic Git maintenance race의 fresh correction을 검증한다.

- Fresh implementation start `P4`: `5dd86afab12ce95d56a604e54424d37ed7cc8067`
- Exact correction `I4`: `9ddd62b230d924876aca8dafb5bdbd978f967cab`
- Approved Micro-SPEC revision: `0efbc069d8d0cbae886c1385c04ea0af21859639`
- 실패한 prior candidate `V3`: `8e0b6a513dd72bf84f4c4462d91915b77bd034af`

Prior hosted run은 DQC PASS evidence가 아니며 same-`V` retry를 수행하지 않는다.

## Checks

- Hosted RED: run `30984256382`, attempt `1`; build PASS, Windows FAIL, Ubuntu FAIL.
- Windows root cause: copied terminal fixture의 working-tree bytes와 committed Git blob bytes가 newline normalization 때문에 달라졌다.
- Ubuntu root cause: synthetic history fixture 생성 중 Git auto-maintenance가 snapshot 이후 object database를 repack했다.
- TDD RED: newline configuration/order와 background maintenance configuration regression `2/2` FAIL.
- Windows correction: temporary fixture repository에 `core.autocrlf=false`를 최초 `git add` 전에 설정한다.
- Synthetic history correction: 최초 fixture commit 전에 `gc.auto=0`, `maintenance.auto=false`를 설정한다.
- Focused history/Windows regression: PASS.
- Exact `I4` full regression: `592/592` PASS.
- `compileall`, `git diff --check`, `proofline validate`: PASS.
- Independent quality/security review: PASS, blocker 없음.
- Independent specification review: PASS, blocker 없음.

## Criteria Results

- `ac-0010`: PASS — Windows temporary fixture bytes와 Git blob identity를 보존하고 hosted exact-wheel gate를 약화하지 않는다. New hosted run identity는 fresh `V4` 이후 DQC에서 별도 검증한다.
- `ac-0021`: PASS — synthetic repository background maintenance만 비활성화하며 production validator의 read-only fail-closed semantics는 변경하지 않는다.

## Result

PASS — exact `I4`가 fresh `P4` 뒤에 있고 local regression·static validation·독립 review를 통과했다. Remote hosted PASS는 fresh `V4` 이후 DQC 조건으로 남는다.
