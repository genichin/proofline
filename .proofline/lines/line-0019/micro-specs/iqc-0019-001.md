---
id: "iqc-0019-001"
micro_spec: "ms-0019-001"
micro_spec_commit: "0efbc069d8d0cbae886c1385c04ea0af21859639"
implementation_commit: "b4fcc5e7e21f2b86488444afcb7afcfed279399c"
result: passed
---

# IQC: Line 0019 fresh Windows hosted correction

## Target

Hosted run `30982884765`에서 발견된 Windows gate fixture history 결함의 fresh correction을 검증한다.

- Fresh implementation start `P3`: `dab028012bb53e180d2de1f4501b11f1ec454a31`
- Exact correction `I3`: `b4fcc5e7e21f2b86488444afcb7afcfed279399c`
- Approved Micro-SPEC revision: `0efbc069d8d0cbae886c1385c04ea0af21859639`
- 실패한 prior candidate `V`: `6ced9f4372c18b628555a22dc39280b4698446ac`

Prior hosted run은 DQC evidence로 승인하지 않으며 same-`V` retry도 수행하지 않는다.

## Checks

- Hosted RED: run `30982884765`, attempt `1`; `build-candidate` PASS, Ubuntu PASS, Windows FAIL.
- Hosted failure: copied `valid-minimal` fixture가 nonterminal `verifying` 상태로 baseline commit되어 installed candidate가 `history.line.policy.missing`을 반환했다.
- Local RED: `test_windows_gate_fixture_is_valid_under_first_parent_policy`가 missing policy assertion으로 FAIL했다.
- Correction: shared fixture bytes는 유지하고 Windows gate의 임시 복사본만 initial commit 전에 terminal `delivered`로 전환한다.
- Independent `/tmp` probe: transformed fixture baseline에 source candidate `proofline validate` exit `0`.
- Focused GREEN: candidate workflow와 Windows runtime tests `22/22` PASS.
- Exact `I3` full regression: `591/591` PASS.
- `compileall`, `git diff --check`, `proofline validate`: PASS.
- Independent quality/security review: PASS, blocker 없음.
- Independent specification review: PASS, blocker 없음.

## Criteria Results

- `ac-0010`: PASS — Windows gate가 installed exact candidate로 current-valid terminal fixture history를 검증한다. New hosted run identity는 fresh `V3` 이후 DQC에서 별도 검증한다.
- `ac-0021`: PASS — source history/runtime regression 및 fail-closed 정책은 유지되며 correction은 Windows temporary fixture chronology에 한정된다.

## Result

PASS — exact `I3`가 fresh cycle `P3` 뒤에 있고, local regression·static validation·독립 review를 통과했다. Remote hosted PASS는 fresh `V3` 이후 DQC 조건으로 남는다.
