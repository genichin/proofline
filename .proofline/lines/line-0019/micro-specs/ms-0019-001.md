---
id: "ms-0019-001"
parent_req: "req-0019"
criteria:
  - "ac-0010"
  - "ac-0021"
spec_status: draft
implementation_status: not_started
---

# Exact hosted candidate DQC와 portable history validation vertical slice

## Scope

### 담당 범위

- `ac-0010` update와 `ac-0021` satisfaction을 하나의 cohesive vertical slice로 구현한다. Generic exact candidate hosted workflow, DQC remote evidence admission, Windows-compatible bounded Git subprocess handling과 full-history source/wheel parity를 함께 닫는다.
- Line 0018 hosted run `30955946035`의 shallow Ubuntu failure와 Windows `os.set_blocking` failure가 새 candidate workflow에서는 각각 checkout precondition과 portable implementation으로 해소되고, 같은 종류의 실패가 DQC 전에 차단됨을 검증한다.

### 제외 범위

- AC chronology 의미, GitHub repository rules, alternate CI providers, controlled end-user Windows 인증, main 자동 merge, release·tag·publication은 담당하지 않는다.
- Line 0018 artifact와 published first-parent history를 수정하지 않는다.

### 기준과 의존성

- Confirmed `dcy-0019`과 approved `req-0019`, updated active `ac-0010`, unchanged active `ac-0021`의 exact revision을 구현 기준으로 사용하며 승인 전에는 구현을 시작하지 않는다.
- Canonical hosted trigger는 `.github/workflows/candidate-verification.yml`의 `candidate/**` remote branch push뿐이다. `proofline-run-dqc`의 `dqc-policy` block은 gate를 mandatory로 선언하고 exact three-job set, attempt-qualified artifact/provenance와 read-back helper identity를 소유한다. Candidate ref는 DQC evidence read-back과 delivery 완료 전까지 보존한다.
- Product source 또는 tests를 변경하기 전에 Line 0019의 별도 persisted `in_progress` transition `P`를 만들고, 구현 commit `I`와 `implemented`/IQC candidate `Q`를 분리한다.

## Implementation

### 변경 대상

- `src/proofline/implementation_history.py`의 bounded Git subprocess execution
- `skills/proofline-start-implementation/scripts/create_worktree.py`의 standalone bounded Git subprocess execution과 focused tests
- `.github/workflows/line-0017-candidate.yml`, `.github/scripts/verify-windows-candidate.ps1` 및 CI contract tests의 generic exact-candidate 전환
- `.github/scripts/verify-candidate-evidence.py`와 missing·duplicate·identity·checksum·staleness negative fixtures
- `docs/contracts/line-delivery.md`, `skills/proofline-run-dqc/SKILL.md`, `templates/schema-v1/artifacts/dqc.md`와 DQC contract tests
- History, source/installed-wheel parity, workflow structure와 DQC evidence regression tests

### 구현 작업

1. Windows-compatible subprocess pump와 generic hosted workflow/DQC evidence에 대한 RED tests를 먼저 작성한다. Native Windows-safe `tests/test_windows_history_runtime.py`, `tests/test_start_implementation_windows_runtime.py` 및 helper fixtures에 `os.set_blocking` 부재, policy-bearing source/installed-wheel multi-commit success, stdout·stderr simultaneous drain, command/session output cap, timeout, nonzero exit, EOF/exit race, terminate/kill/reap과 standalone preflight parity를 포함한다.
2. Validator와 standalone preflight가 system Python import 제약을 지키면서 동일한 portable bounded subprocess contract를 사용하도록 구현한다. POSIX·Windows 모두에서 existing deadlines, aggregate budgets, Git environment hardening, fail-closed diagnostic과 read-only snapshots를 유지한다.
3. Workflow를 `.github/workflows/candidate-verification.yml`로 전환하고 `candidate/**` push 이외 trigger를 제거한다. `build-candidate`, `ubuntu-python311`, `windows-python311`은 event full SHA를 exact ref로 full-history checkout하고 credentials를 보존하지 않으며 checked-out HEAD를 assert한다.
4. Exact candidate에서 wheel 하나, strict `SHA256SUMS`와 schema-v1 `CANDIDATE_PROVENANCE.json`을 한 번 생성해 `proofline-candidate-{run_id}-{run_attempt}` artifact로 올린다. Ubuntu·Windows가 같은 run attempt artifact의 wheel identity를 독립 검증하고 policy-bearing source·installed-wheel history validation과 required regression을 실행한다.
5. `.github/scripts/verify-candidate-evidence.py`에 exact workflow/event/head SHA/run attempt, terminal success required jobs, expected-name non-expired single artifact, provenance schema, strict checksum, wheel digest와 `V..DQC` allowed-path staleness의 bounded read-back을 구현한다. Missing·duplicate·wrong identity·non-success·expired·malformed-extra·mismatch·ref drift·stale-source matrix를 nonzero로 거부한다.
6. DQC contract·skill·template를 updated `ac-0010`과 동기화한다. 기존 six required checks와 four conditional triggers를 보존하고 mandatory hosted block·attempt identity·helper command를 추가한다. Hosted non-PASS·누락·불완전 evidence는 원인과 무관하게 blocked로 유지한다. Candidate defect/source 변경은 `verifying → in_progress → fresh P→I→Q→V`를 요구하며 same-`V` rerun·retry admission이나 transient waiver는 구현하지 않는다.
7. Source CLI, isolated installed wheel과 standalone skill tests, full canonical regression, static workflow assertions와 recursive no-mutation snapshot을 실행한다. IQC에는 `I`에서 이용 가능한 local/source/wheel focused evidence만 기록한다. IQC/Q 후 hosted candidate `V`를 remote branch에 push하고 GitHub API와 downloaded artifact를 independently read-back한 run·attempt·job·artifact·wheel identity는 DQC Checks에만 기록한다.

### Rollback과 containment

- Implementation branch에서 correction을 철회할 때 source·workflow·contract changes만 되돌리고 Line 0018 history, main, tag, release와 publication을 변경하지 않는다.
- Hosted workflow는 read-only permissions를 사용하고 candidate ref, main 또는 lifecycle artifact를 자동 변경·삭제하지 않는다. Failed run은 DQC PASS 권한을 갖지 않으며 candidate ref cleanup은 delivery 후 별도 명시적 action으로 제한한다.

## Verification

- 검사: focused RED/GREEN subprocess matrix, native hosted Windows Python 3.11의 두 exact Windows-safe test files와 installed executable fixture, Ubuntu full-history canonical validation, source/isolated-wheel parity, generic workflow static contract, DQC skill/template/contract tests, read-back helper negative matrix, full regression과 Git/index/worktree/HEAD/refs/object database read-only snapshot을 실행한다. Exact candidate run은 API와 artifact download로 workflow/event/head SHA, run attempt, exact three-job conclusion, attempt-qualified single artifact identity와 strict wheel checksum을 read-back한다.
- 기대 결과: Windows와 POSIX가 동일 valid history를 PASS하고 shallow·missing history, timeout, output cap, nonzero exit와 cleanup failure를 stable fail-closed 결과로 처리한다. Candidate workflow는 exact full-history SHA의 wheel을 한 번 build하여 Ubuntu와 Windows에서 동일 digest로 PASS하며, remote evidence 누락·실패·drift·mismatch·staleness에서는 DQC-ready가 되지 않는다.
- Evidence: `.proofline/lines/line-0019/micro-specs/iqc-0019-001.md`에는 exact approved Micro-SPEC commit, `P`, implementation commit `I`, RED/GREEN local results, source/wheel parity와 no-mutation result만 기록한다. IQC/Q 이후 생성되는 hosted run·attempt·job·artifact·wheel identities는 `.proofline/lines/line-0019/dqc-0019.md` Checks에서 exact `V`와 함께 independently read-back해 기록한다.
