---
id: "ms-0020-001"
parent_req: "req-0020"
criteria:
  - "ac-0003"
  - "ac-0007"
  - "ac-0010"
  - "ac-0021"
  - "ac-0022"
spec_status: approved
implementation_status: in_progress
---

# Main-owned handoff와 concurrent Line integration vertical slice

## Scope

### 담당 범위

- `ac-0003`, `ac-0007`, `ac-0021` update, `ac-0022` creation과 `ac-0010` satisfaction을 하나의 cohesive governance vertical slice로 구현한다. Delayed Micro-SPEC coverage, main-owned exact handoff, independent Micro-SPEC review·user approval, Line별 first-parent evidence와 main-first integration candidate를 함께 닫는다.
- Line 0020은 current immediate-coverage validator를 변경하는 migration이므로 이 Micro-SPEC을 main에서 bootstrap draft·approve하지만 제품 source/test 구현은 exact-H Line worktree의 별도 `P` 이후에만 수행한다.

### 제외 범위

- 구현 에이전트의 main mutation, self-approval, reviewer mutation, multi-Line conflict 자동 해결, octopus merge, history rewrite·rebase·squash·cherry-pick, existing delivered Line rewrite와 release·tag·publication은 담당하지 않는다.
- Integration merge에서 manifest 이외 source·test·build/runtime conflict resolution을 제공하지 않는다.

### 기준과 의존성

- Reconfirmed `dcy-0020`, approved `req-0020`, updated active `ac-0003`·`ac-0007`·`ac-0021`, new active `ac-0022`, unchanged active `ac-0010`과 approved bootstrap Micro-SPEC의 combined exact specification baseline `S=A`를 구현 기준으로 사용한다.
- Governance lead가 main에서 status-only Line handoff `H`를 `A`의 direct first-parent child로 기록하고 exact-H worktree를 검증한 뒤 fresh implementation agent에게 인계한다.
- Product source/test/document 변경 전에 이 Micro-SPEC의 별도 `implementation_status: in_progress` commit `P`를 만들며 Line 0020의 `S=A < H < P < I < Q` first-parent chronology를 유지한다. Post-H `S0/S`는 만들지 않는다.

## Implementation

### 변경 대상

- `src/proofline/validator.py`의 lifecycle-aware criteria coverage와 integration manifest reference/schema validation
- `src/proofline/implementation_history.py`의 Line-spine/main-spine discovery, temporal admission과 immutable historical validation
- `skills/proofline-start-implementation/SKILL.md` 및 `scripts/create_worktree.py`의 `A → H → exact-H worktree`, role handoff와 exact-H recovery
- `skills/proofline-run-dqc/SKILL.md`, candidate evidence helper와 canonical DQC flow의 main-first integration candidate admission
- `docs/contracts/requirements-and-criteria.md`, `line-delivery.md`, `micro-spec-and-iqc.md`, artifact layout와 canonical integration manifest template/schema
- Source·isolated-wheel·standalone resource parity, Real-Git ancestry, no-mutation, legacy compatibility와 candidate workflow tests

### 구현 작업

1. Immediate coverage, approval-commit worktree와 global-first-parent assumptions을 드러내는 RED tests를 먼저 작성한다. Zero/partial drafting, first `P` coverage gate, exact `A → H`, post-H retry, independent `S0 → S`, diverged main/Line graph와 manifest authority matrix를 포함한다.
2. Reference validator가 direct references와 Micro-SPEC subset을 항상 검사하되 Line/Micro-SPEC lifecycle에 따라 full coverage enforcement를 지연하도록 구현한다. First implementation transition과 verifying/delivered 상태의 uncovered target은 stable `criteria.uncovered`로 fail-closed한다.
3. Standalone worktree workflow가 exact approved REQ·AC baseline `A`, direct status-only handoff `H`, clean main과 collisions을 검사하고 exact H에서 worktree를 생성·read-back하도록 동기화한다. Post-H failure는 history를 rewrite하지 않고 same-H idempotent retry 또는 cancellation으로 제한한다.
4. Micro-SPEC draft author, independent read-only reviewer, user-only approval authority와 status-only `S` recorder의 exclusive phase를 contract·skill·tests에 고정한다. Line 0020 combined bootstrap `S=A`와 후속 Line `S0 → S`를 구분하고 governance-lead 단독 approval, stale draft, body-changing approval, self-approval과 concurrent mutation을 거부한다.
5. `.proofline/lines/line-NNNN/integration-NNNN.md` schema와 parser를 추가한다. Main-first two-parent `V`의 containing manifest가 exact `line_id`, `M`, `Q`와 actual parent order를 결속하고 deterministic merge result 외 변경을 거부하도록 구현한다.
6. History validator가 Line 0020 `S=A → H → P → I → Q`, 후속 Line `A → H → S0 → S → P → I → Q`, candidate의 `parent[0]=M` main spine과 `parent[1]=Q` Line spine, post-integration DQC/delivery chronology를 context별로 검증하도록 구현한다. Arbitrary second parent는 허용하지 않고 existing linear/legacy policy·cutoff·rework matrix를 보존한다.
7. DQC workflow와 exact hosted candidate evidence를 merge candidate `V`에 적용한다. Pre-integration main/Line ref drift에는 same-Q fresh V와 fresh hosted/DQC를 요구하고 post-integration은 immutable object binding만 사용한다.
8. Full source regression, isolated built-wheel CLI, packaged standalone script, Real-Git parent/manifest/staleness matrix, recursive no-mutation snapshot과 existing Line 0018·0019 compatibility를 검증한다.

### Rollback과 containment

- Correction은 Line worktree와 disposable integration candidate branch에 한정한다. Main, existing Line history, remote main, candidate refs, tags와 releases를 자동 변경하지 않는다.
- Candidate merge가 conflict하거나 manifest 외 merge-only 제품 변경이 필요하면 V를 만들지 않고 owner Line으로 반환한다. Failed hosted/DQC evidence는 integration authority를 갖지 않는다.

## Verification

- 검사: Focused RED/GREEN criteria coverage·handoff·approval tests, Real-Git Line/main spine와 integration manifest positive/negative matrix, source/isolated-wheel CLI parity, standalone worktree resource execution, existing full regression, `compileall`, canonical validation, diff check와 Git/index/worktree/ref/object no-mutation snapshot을 실행한다. Exact `V`는 hosted Ubuntu·Windows required jobs와 artifact/provenance/checksum read-back을 통과해야 한다.
- 기대 결과: Future Line은 no/partial Micro-SPEC 상태로 exact-H worktree를 만들고 first `P` 전에 full coverage와 target approval을 강제한다. Main이 concurrent 진행해도 exact `M`·`Q` manifest candidate와 각 spine이 PASS하며 drift·parent mismatch·arbitrary second parent·merge-only product change·stale approval은 stable failure가 된다. Existing linear delivered Lines와 current hosted DQC guarantees는 regression 없이 PASS한다.
- Evidence: `.proofline/lines/line-0020/micro-specs/iqc-0020-001.md`에 exact approved Micro-SPEC commit, `P`, implementation commit `I`, focused/full source·wheel·Real-Git/no-mutation 결과를 기록한다. Exact merge candidate `V`의 hosted run·job·artifact·wheel identity와 integration manifest `M`·`Q` binding은 `.proofline/lines/line-0020/dqc-0020.md`에 기록한다.
