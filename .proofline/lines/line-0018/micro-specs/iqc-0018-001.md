---
id: "iqc-0018-001"
micro_spec: "ms-0018-001"
micro_spec_commit: "6def2ae8d8abd5ca7faa3626ec82f40b78916059"
implementation_commit: "a1aef9439561f5cf5c590e6b291c2979af760f04"
result: passed
---

# IQC: First-parent implementation history validator

## Target

Approved `ms-0018-001`의 exact first-parent lifecycle validator vertical slice를 implementation commit `a1aef9439561f5cf5c590e6b291c2979af760f04`에서 검증한다. 이 IQC는 Micro-SPEC 구현 검증만 승인하며 DQC, main integration, Line delivery, release 또는 publication을 승인하지 않는다.

Lifecycle chronology:

| Boundary | Commit | Evidence |
| --- | --- | --- |
| Approved Micro-SPEC `M` | `6def2ae8d8abd5ca7faa3626ec82f40b78916059` | Terminal legacy amendment가 반영된 exact approved specification revision |
| Persisted `in_progress` `P` | `cc410d7608961cbcb8ebe8b3aec0613dfbe1b99f` | 제품 변경과 분리된 lifecycle-only commit |
| Policy baseline `B` | `031c91dc6e21497bf814436e89730e3d5f5550c5` | Line 0018 `implementation_history: first_parent` 최초 persisted commit |
| Implementation `I` | `a1aef9439561f5cf5c590e6b291c2979af760f04` | Exact independently reviewed implementation commit |

검사한 ordering은 `M < P < B < I`이며, 이 IQC와 `implemented` transition을 소유하는 후속 Q는 `I`와 분리한다.

## Checks

| 검사 | 실행 명령 또는 방법 | Exit code·판정 | 결과 요약 | Evidence |
| --- | --- | --- | --- | --- |
| Full source regression | `uv run pytest -o addopts='' -q` | exit 0, PASS | 539 passed | Exact `I` local execution |
| Source canonical validation | `PYTHONPATH=src uv run proofline validate` | exit 0, PASS | Canonical artifact·topology·first-parent history validation 통과 | Exact `I` local execution |
| Source syntax·diff hygiene | `python -m compileall -q src tests`; `git diff --check` | 모두 exit 0, PASS | Source와 test compile 및 diff hygiene 통과 | Exact `I` local execution |
| Isolated wheel build | `uv build --wheel --out-dir /tmp/line0018-iqc-dist` | exit 0, PASS | `proofline-0.5.0-py3-none-any.whl` 생성 | Exact `I` local execution |
| Wheel artifact digest | `sha256sum /tmp/line0018-iqc-dist/proofline-0.5.0-py3-none-any.whl` | exit 0, PASS | SHA-256 `ad5eb9a60e0ee7ad5d8a73300daeb2e98a9e423bf3609d64ac5f51ce8745d280` | Local build read-back |
| Isolated installed-wheel validation | Fresh Python 3.11 venv에 exact wheel 설치 후 `proofline validate` | exit 0, PASS | Source candidate와 동일한 canonical history validation 통과 | `/tmp/line0018-iqc-venv` isolated execution |
| Validation read-only snapshot | `.proofline`, Git metadata·refs·status의 validation 전후 recursive digest 비교 | PASS | Before/after byte-identical; digest `e1ae049c42d5aeee67362f08f577340ac55ee5748bc39079ec25a972a6f6c8c3` | Source·wheel validation session |
| Independent specification review | Exact `B..I` read-only review | PASS | blockers `[]`, nonblocking `[]`, mutation `false` | Reviewed head `a1aef9439561f5cf5c590e6b291c2979af760f04` |
| Independent quality/security review | Exact `B..I` read-only review | PASS | security concerns `[]`, logic errors `[]`, suggestions `[]`, mutation `false` | Reviewed head `a1aef9439561f5cf5c590e6b291c2979af760f04` |

## Criteria Results

| AC | 판정 | 근거 |
| --- | --- | --- |
| `ac-0021` | passed | 전체 first-parent lifecycle cycle, policy provenance, terminal legacy cutoff, exact implementation/IQC binding, unavailable-history fail-closed, Line-init transaction, source/isolated-wheel parity 및 read-only behavior가 exact `I`에서 검증됐다. |

## Result

- 전체 판정: passed
- 요약: `ms-0018-001`의 구현이 exact approved revision과 implementation commit에 결속된 source·isolated-wheel·read-only 검증 및 독립 review를 통과했다.
- 경계: 이 PASS는 Q admission을 위한 IQC이며 DQC, main fast-forward, Line delivery, remote push, release, tag 또는 publication 승인이 아니다.
