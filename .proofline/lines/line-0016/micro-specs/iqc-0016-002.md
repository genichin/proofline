---
id: "iqc-0016-002"
micro_spec: "ms-0016-002"
micro_spec_commit: "4b6ab5ede70af0633b6c2d889a6d422633d0285d"
implementation_commit: "9c5aecc6f59c296e77c602c1acdcb56e2c721eb9"
result: passed
---

# IQC: Allocation reservation과 Line scaffold transaction

## Target

승인된 `ms-0016-002`의 ledger reservation·Line/Discovery owned transaction, dry-run, concurrency·rollback·descriptor finalization 및 source·installed-wheel E2E를 exact implementation commit에서 검증한다.

## Checks

| 검사 | 실행 명령 또는 방법 | Exit code·판정 | 결과 요약 | Evidence |
| --- | --- | --- | --- | --- |
| Transaction·wheel focused regression | `uv run pytest -q tests/test_line_init.py tests/test_wheel_package.py` | exit 0, PASS | 72 passed | implementation commit `9c5aecc6f59c296e77c602c1acdcb56e2c721eb9`의 repository tests |
| 전체 source regression | `uv run pytest -q` | exit 0, PASS | 298 passed | 같은 implementation commit의 전체 suite |
| Canonical validation·compile | `PYTHONPATH=src uv run proofline validate`; `uv run python -m compileall -q src tests`; `git diff --check` | 모두 exit 0, PASS | Canonical artifact와 Python source 및 diff hygiene 통과 | 같은 implementation commit의 command evidence |
| Exact installed-wheel gate | `uv build --wheel`; isolated venv non-editable install; `python -I` provenance; installed `proofline validate` | exit 0, PASS | Wheel SHA-256 `21d7eb34d15f02e8879149ffd2da261210939387ec003c81e69c3bb96f3c9052`; module origin은 isolated `site-packages`; installed validator PASS | source SHA `9c5aecc6f59c296e77c602c1acdcb56e2c721eb9`에서 생성한 exact wheel 및 `tests/test_wheel_package.py` |

## Criteria Results

| AC | 판정 | 근거 |
| --- | --- | --- |
| `ac-0004` | passed | Fresh·legacy migration, reservation+pair transaction, authority, dry-run, race·fault·rollback, ref deletion 및 installed-wheel matrix 72건이 통과했다. |

## Result

- 전체 판정: passed
- 요약: 승인된 MS002 transaction·wheel 범위가 exact implementation commit에서 통과했으며 남은 blocker가 없다.
