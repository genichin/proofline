---
id: ms-0012-004
parent_req: req-0012
criteria:
  - ac-0016
  - ac-0017
spec_status: approved
implementation_status: implemented
---

# User harness initialization AC 통합

## Scope

### 담당 범위

- `ac-0017`의 idempotency·fail-closed·atomicity 조건을 `ac-0016`의 명시적 user harness initialization에 통합한다.
- `ac-0017`을 `ac-0016`으로 대체된 초기화 안전 조건 AC로 retire한다.

### 제외 범위

- Home writer와 CLI의 제품 코드 및 test 변경
- User resource migration 또는 수정 파일 merge

### 기준과 의존성

- `ac-0016`, `ac-0017`의 현재 active revision과 Line 0010 evidence
- User home ownership, managed resource와 application no-mutation 계약

## Implementation

### 변경 대상

- `.proofline/criteria/ac-0016.md`
- `.proofline/criteria/ac-0017.md`

### 구현 작업

1. Packaged canonical resource와 manifest 설치 조건을 보존한다.
2. Exact rerun no-op, mismatch·unexpected entry·symlink·concurrent target·write failure 안전 조건을 통합한다.
3. `ac-0017`에 survivor와 retirement 사유를 기록하고 `retired`로 전환한다.
4. Existing Line 0010 evidence와 canonical reference를 확인한다.

### Rollback과 containment

- 설치 topology 또는 failure-atomicity 조건이 누락되면 두 AC 변경을 함께 되돌리고 retire하지 않는다.

## Verification

- 검사: 두 AC condition matrix, Line 0010 evidence 대조, 직접 참조 검색, `proofline validate`, `git diff --check`
- 기대 결과: `ac-0016`이 명시적·idempotent·fail-closed user harness initialization 전체를 소유하고 `ac-0017`은 고정 경로에 retired 상태로 남는다.
- Evidence: `iqc-0012-004.md`
