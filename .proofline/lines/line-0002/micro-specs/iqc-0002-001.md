---
id: iqc-0002-001
micro_spec: ms-0002-001
micro_spec_commit: "640e0c2e0c722960bd2063eac7f338f906d73f9f"
implementation_commit: "4e79f6f5c2c37765dcd7664eab4f8a534f89c03d"
result: passed
---

# IQC: Deterministic Line bootstrap CLI

## Target

- `ms-0002-001`의 `ac-0004`, `ac-0005`
- `proofline line init LINE_ID --title TITLE [--dry-run]`
- Canonical template wheel resource, explicit ID, Git-history 재사용 금지, symlink·충돌 preflight와 실패 시 무변경 보장

## Checks

| 검사 | 실행 명령 또는 방법 | Exit code·판정 | 결과 요약 | Evidence |
| --- | --- | --- | --- | --- |
| CLI·safety·wheel test | `uv run pytest tests/test_line_init.py tests/test_wheel_package.py -q` | `0` · PASS | 13개 test 통과 | `tests/test_line_init.py`, `tests/test_wheel_package.py` |
| 실제 project dry-run | `uv run proofline line init line-0003 --title 'IQC dry-run' --dry-run` | `0` · PASS | 두 canonical 예정 path를 보고하고 파일을 생성하지 않음 | command stdout 및 implementation commit |
| Dry-run 무변경 | 실행 전후 `git status --porcelain=v1 --untracked-files=all` 비교 | PASS | working tree snapshot 동일 | IQC 실행 command |
| Canonical validation | `uv run proofline validate` | `0` · PASS | 진단 없음 | implementation commit |
| Wheel template resource | wheel build 후 archive와 isolated import 검사 | PASS | Line·Discovery canonical Markdown resource 포함 및 읽기 성공 | `tests/test_wheel_package.py` |

## Criteria Results

| AC | 판정 | 근거 |
| --- | --- | --- |
| `ac-0004` | passed | source CLI와 built wheel resource에서 생성 template, ID·status·제목 및 validator 통과를 자동화 test로 확인했다. |
| `ac-0005` | passed | dry-run, invalid ID/title, current collision, historical ID, symlink 및 rename race가 기존 tree를 변경하지 않고 안정적으로 실패하는 것을 확인했다. |

## Result

- 전체 판정: passed
- 요약: 승인된 CLI Micro-SPEC의 성공·실패·무변경 조건을 exact implementation commit에서 모두 충족했다.
