---
id: ms-0002-002
parent_req: req-0002
criteria:
  - ac-0006
spec_status: draft
implementation_status: not_started
---

# Hermes evidence-based Discovery workflow skill

## Scope

### 담당 범위

- `ac-0006`을 충족하는 repository-owned Hermes `SKILL.md`를 제공한다.
- CLI scaffold 이후 직접 evidence 조사, canonical section 작성, Open Question 처리와 draft-only authority를 재사용 가능한 절차로 정의한다.

### 제외 범위

- Live profile 설치와 skill registry publish
- CLI 구현과 LLM/provider runtime embedding
- Discovery confirmation 및 후속 REQ/AC/Micro-SPEC 생성

### 기준과 의존성

- 기준 specification commit은 `d59a99fc00dfb98086e2dbd9472fcf0eb0b96a1a`이다.
- Hermes의 현재 `SKILL.md` frontmatter contract와 repository-owned source 관행을 따른다.
- Skill은 ProofLine CLI, 직접 source inspection 및 `docs/artifact-layout.md`를 사용하며 특정 model provider에 의존하지 않는다.

## Implementation

### 변경 대상

- `skills/proofline-start-line/SKILL.md`
- Skill structure와 금지 authority를 검사하는 `tests/` contract test

### 구현 작업

1. Skill의 필수 frontmatter, workflow 단계와 금지 authority를 표현하는 정적 contract test를 먼저 작성하고 RED를 확인한다.
2. Trigger, prerequisite, scaffold command, evidence 조사 우선순위, section별 작성 규칙 및 Open Question gate를 작성한다.
3. 사용자가 결정해야 하는 내용을 임의로 해소하거나 `confirmed`로 전환하지 않도록 명시적인 stop condition을 둔다.
4. 실패 시 partial draft를 검증하고 수정하거나 변경 전 상태를 보존하는 절차와 verification checklist를 작성한다.
5. Skill format, ProofLine validator 및 전체 test를 실행한다.

### Rollback과 containment

- Skill은 repository source만 추가하며 live Hermes profile을 변경하지 않는다.
- Rollback은 skill source와 contract test를 제거하는 것으로 제한되고 기존 CLI behavior에는 영향이 없다.

## Verification

- 검사: skill frontmatter·필수 section·권한 경계를 검사하는 test, `uv run proofline validate`, `uv run pytest`를 실행한다.
- 기대 결과: evidence-first draft workflow가 완전하며 LLM-in-CLI, 자동 confirmation, live-profile write를 지시하지 않는다.
- Evidence: IQC artifact `iqc-0002-002.md`에 exact Micro-SPEC commit, implementation commit, command와 결과를 기록한다.
