---
id: ms-0009-002
parent_req: req-0009
criteria:
  - ac-0011
  - ac-0013
  - ac-0014
  - ac-0015
spec_status: approved
implementation_status: in_progress
---

# Official v0.2.2 installer delivery

## Scope

### 담당 범위

- Distribution version·lock 0.2.2
- Exact candidate wheel·checksum build와 publication
- Tagged raw installer fresh remote install
- v0.2.1 production updater→v0.2.2 compatibility transaction

### 제외 범위

- install.sh Release upload asset
- Existing release mutation

### 기준과 의존성

- MS-0009-001 IQC가 passed여야 publication한다.
- v0.2.2 Release assets는 wheel·SHA256SUMS 두 개뿐이다.
- `0.2.2`, tag, candidate와 asset identity는 AC가 아니라 이 delivery의 implementation·verification parameter다.
- AC-0011, AC-0013과 AC-0015는 version-specific AC correction의 retirement evidence로만 담당한다.

## Implementation

### 변경 대상

- `pyproject.toml`, `uv.lock`, version·wheel tests, release evidence

### 구현 작업

1. Version·lock·tests를 0.2.2로 동기화한다.
2. Exact candidate wheel·checksum과 fresh local install을 검증한다.
3. User publication approval 후 immutable tag·Release를 게시한다.
4. Public raw installer와 v0.2.1 production updater transaction을 실행한다.

### Rollback과 containment

- Existing v0.2.2 object conflict는 overwrite·delete하지 않고 실패한다.
- Partial publication은 exact successful objects를 보존하고 누락 단계만 재개한다.

## Verification

- 검사: version·wheel·checksum·tag·asset allowlist·public installer·production updater·application snapshots
- 기대 결과: exact v0.2.2 release가 one-line fresh install과 v0.2.1 update를 모두 지원함
- Evidence: `iqc-0009-002.md`, `dqc-0009.md`, `https://github.com/genichin/proofline/releases/tag/v0.2.2`
