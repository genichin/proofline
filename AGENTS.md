# ProofLine 에이전트 지침

ProofLine은 AI 에이전트, 특히 Hermes Agent의 소프트웨어 개발을 돕는 스킬 모음 및 하네스이다.

## 기본 원칙

- 불가피한 경우 또는 영어로 써야 의미가 제대로 전달되는 경우를 제외하고 모든 설명, 문서, 작업 기록은 한글로 작성한다.
- 작업 전에 관련 파일과 기존 규칙을 먼저 확인한다.
- 변경은 요청 범위 안에서 작고 명확하게 유지한다.
- 구현 후 가능한 검증(테스트, 린트, 실행 확인 등)을 수행하고 결과를 사실대로 보고한다.
- 기존 동작이나 사용자 데이터를 임의로 삭제하거나 파괴하지 않는다.
- 불확실한 내용은 추측하지 말고 확인하거나 명시적으로 질문한다.

## ProofLine 산출물

- 프로젝트별 ProofLine 설정은 프로젝트 루트의 `proofline.yaml`에 기록한다.
- ProofLine을 적용한 프로젝트의 영속 산출물은 기본적으로 프로젝트 루트의 `.proofline/` 아래에 저장한다.
- ProofLine contract의 진입점은 [`docs/artifact-layout.md`](docs/artifact-layout.md)이며, 작업별 상세 계약은 이 문서의 탐색표를 따라 선택적으로 읽는다.
- 구현 코드, 스킬, 템플릿에서 경로를 정할 때 문서와 다른 구조를 임의로 만들지 않는다.
- `temp/` 아래 자료는 참고용이며 ProofLine의 정본 산출물이나 검증 입력으로 취급하지 않는다.
